apiVersion: v1
kind: Pod
spec:
  containers:
    - name: agent
      image: stembord/build-agent-elixir:1.8
      imagePullPolicy: Always
      command:
        - cat
      tty: true
      volumeMounts:
        - name: dockersock
          mountPath: /var/run/docker.sock
  volumes:
    - name: dockersock
      hostPath:
        path: /var/run/docker.sock
