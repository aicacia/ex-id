apiVersion: v1
kind: Service
metadata:
  name: { { include "aicacia-id.fullname" . } }
  labels: { { - include "aicacia-id.labels" . | nindent 4 } }
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "aicacia-id.selectorLabels" . | nindent 4 } }
